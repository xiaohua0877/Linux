4.4.1 MCS锁机制
 
    假设在一个锁争用激烈的系统中。所有自旋等待锁的线程都在同时一个共享变量上自旋，申请和释放锁都 
在同时一个变量上修改。由于cache一致性原理。导致参与自旋的CPU中cacheline变得无效。在锁争用激烈过程中
导致严重的CPU高速缓存行颠簸现象（cpu cacheling bouncing)现象，即多个CPU上的cache line反复失效。
大大降低系统整体性能。
    MCS 算法可以解决自旋锁遇到的问题。
